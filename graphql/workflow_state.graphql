type WorkflowState implements BaseDocType {
  doctype: String
  name: String
  owner: User!
  creation: String
  modified: String
  modified_by: User!
  docstatus: Int
  idx: Int
  owner__name: String!
  modified_by__name: String!
  parent__name: String
  workflow_state_name: String!
  icon: String
  style: WorkflowStateStyleSelectOptions
}

enum WorkflowStateStyleSelectOptions {
  PRIMARY
  INFO
  SUCCESS
  WARNING
  DANGER
  INVERSE
}

enum WorkflowStateSortField {
  NAME
  CREATION
  MODIFIED
  WORKFLOW_STATE_NAME
}

input WorkflowStateSortingInput {
  direction: SortDirection!
  field: WorkflowStateSortField!
}

type WorkflowStateCountableEdge {
  cursor: String!
  node: WorkflowState!
}

type WorkflowStateCountableConnection {
  pageInfo: PageInfo!
  totalCount: Int
  edges: [WorkflowStateCountableEdge!]!
}

extend type Query {
  WorkflowState(name: String!): WorkflowState!
  WorkflowStates(filter: [DBFilterInput], sortBy: WorkflowStateSortingInput, before: String, after: String, first: Int, last: Int): WorkflowStateCountableConnection!
}
