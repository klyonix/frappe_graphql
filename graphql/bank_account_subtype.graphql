type BankAccountSubtype implements BaseDocType {
  doctype: String
  name: String
  owner: User!
  creation: String
  modified: String
  modified_by: User!
  docstatus: Int
  idx: Int
  owner__name: String!
  modified_by__name: String!
  parent__name: String
  account_subtype: String
}

enum BankAccountSubtypeSortField {
  NAME
  CREATION
  MODIFIED
  ACCOUNT_SUBTYPE
}

input BankAccountSubtypeSortingInput {
  direction: SortDirection!
  field: BankAccountSubtypeSortField!
}

type BankAccountSubtypeCountableEdge {
  cursor: String!
  node: BankAccountSubtype!
}

type BankAccountSubtypeCountableConnection {
  pageInfo: PageInfo!
  totalCount: Int
  edges: [BankAccountSubtypeCountableEdge!]!
}

extend type Query {
  BankAccountSubtype(name: String!): BankAccountSubtype!
  BankAccountSubtypes(filter: [DBFilterInput], sortBy: BankAccountSubtypeSortingInput, before: String, after: String, first: Int, last: Int): BankAccountSubtypeCountableConnection!
}
