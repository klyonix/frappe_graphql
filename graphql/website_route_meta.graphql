type WebsiteRouteMeta implements BaseDocType {
  doctype: String
  name: String
  owner: User!
  creation: String
  modified: String
  modified_by: User!
  docstatus: Int
  idx: Int
  owner__name: String!
  modified_by__name: String!
  parent__name: String
  meta_tags: [WebsiteMetaTag!]!
}

enum WebsiteRouteMetaSortField {
  NAME
  CREATION
  MODIFIED
}

input WebsiteRouteMetaSortingInput {
  direction: SortDirection!
  field: WebsiteRouteMetaSortField!
}

type WebsiteRouteMetaCountableEdge {
  cursor: String!
  node: WebsiteRouteMeta!
}

type WebsiteRouteMetaCountableConnection {
  pageInfo: PageInfo!
  totalCount: Int
  edges: [WebsiteRouteMetaCountableEdge!]!
}

extend type Query {
  WebsiteRouteMeta(name: String!): WebsiteRouteMeta!
  WebsiteRouteMetas(filter: [DBFilterInput], sortBy: WebsiteRouteMetaSortingInput, before: String, after: String, first: Int, last: Int): WebsiteRouteMetaCountableConnection!
}
